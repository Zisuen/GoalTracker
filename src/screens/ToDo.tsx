import React from 'react';
import {Text} from 'react-native';
import Layout from '../components/Layout';

const ToDo = () => {
  return (
    <Layout>
      <Text>ToDo</Text>
    </Layout>
  );
};

export default ToDo;
