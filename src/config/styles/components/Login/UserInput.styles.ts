import {StyleSheet} from 'react-native';

const stylesUserInput = StyleSheet.create({
  formContainer: {
    marginBottom: 12,
  },
  formTitle: {
    fontSize: 22,
    textTransform: 'capitalize',
  },
  formInput: {
    marginTop: 5,
    fontSize: 20,
  },
});

export default stylesUserInput;
